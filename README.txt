Terminal Commands:

//if build is not set
 mkdir build 			//create the build directory
 cmake -B .\build\		//build the directory

//if built, compile the changes and run
 cmake --build .\build\
 .\build\Debug\cmkDNM.exe
